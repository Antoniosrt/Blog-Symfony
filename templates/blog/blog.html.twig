{% extends '/base.html.twig' %}
{% block menu %}
{% endblock %}
 {% block body %}

            <div class="ml-4 mt-4 row no-gutters">
                <div class="ml-3  col-sm-12">
                <span class="cat">Categoria: Todos</span>
                    <hr class="hr-cat p-0 mt-1">
                </div>


            </div>
             <div class="row ml-5 no-gutters">
    {% for post in posts %}

                 <div class="col-sm-4 mb-3">
                     <a class="text-decoration-none  " href="post/view/{{ post.id }}">
                     <div class="card bg-dark post text-white" style="width: 18rem;">
                         <img class="card-img w-100 "  src="/images/{{ post.imagem }}" alt="Imagem de capa do card">
                         <div class="card-header">
                             <h5 class="card-title">{{ post.title }}</h5>
                             <p class="text-muted text-white">{{ post.cat }}</p>
                         </div>
                     </div>
                     </a>
                 </div>

             {% endfor %}
             </div>
         </div>
         </div>




 {% endblock %}